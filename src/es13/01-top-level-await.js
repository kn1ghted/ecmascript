// Enables the keyword 'await' at the top level, without the restriction to be called only from an asynchronous function
import { products } from "./products.js";

console.log(products);
console.log('After Products');