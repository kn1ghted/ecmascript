// String padding
let string = 'HEY!';

// Pads the string, completing a max lenght as first parameter with the given string at the start of the oringinal string
console.log(string.padStart(8,'*'));

// Pads the string, completing a max lenght as first parameter with the given string at the end of the original string
console.log(string.padEnd(8,'*'));